<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tes.theengineeringsolutions">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".activities.MyApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.admin.ViewStudentResultActivity"/>
        <activity android:name=".activities.admin.StudentsResults" />
        <activity android:name=".activities.admin.UploadNotesActivity" />
        <activity android:name=".activities.admin.PostMessageActivity" />
        <activity android:name=".activities.about.About" />
        <activity android:name=".activities.about.Developer" />
        <activity android:name=".activities.quiz.UploadResultsActivity" />
        <activity android:name=".activities.quiz.QuizResult" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            android:name="DATABASE"
            android:value="LocalTestDatabase.db" />
        <meta-data
            android:name="VERSION"
            android:value="2" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.tes.theengineeringsolutions.Models" />

        <activity android:name=".activities.quiz.QuizActivity" />
        <activity android:name=".activities.auth.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.admin.UploadTestFile" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.auth.LoginActivity" />
        <activity android:name=".activities.auth.ForgetPasswordActivity" />
        <activity android:name=".activities.auth.SignupActivity" />

        <receiver
            android:name=".Models.ConnectivityReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>